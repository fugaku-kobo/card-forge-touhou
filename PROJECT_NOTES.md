# Card Forge — 手塚カードゲーム制作ツール

## プロジェクト概要

「手塚カードゲーム(仮)」という自作TCG用のカード制作Webアプリ。手塚治虫作品(ブラック・ジャック、鉄腕アトム)をモチーフにしたカードゲーム。

- **公開URL:** https://fugaku-kobo.github.io/card-forge/
- **GitHub:** https://github.com/fugaku-kobo/card-forge
- **メインファイル:** `index.html` (1ファイルで完結)
- **使用技術:** HTML + CSS + JavaScript (バニラ)、jsPDF、html2canvas、SheetJS(xlsx)、PeerJS(二台対戦のWebRTC)

## 主要機能

1. **カード制作**: タイプ別の入力フォーム(主人公/キャラ/展開/テコ入れ/どんでん返し)
2. **Excelインポート**: 手塚カードゲーム形式のxlsxを読み込み
3. **カードレイアウト**: 設計図に沿った3種のレイアウト
   - 主人公: レベル+名前+大イラスト+スキル2つ
   - キャラ: レベル+IP+名前+HP+イラスト+技2つ+作品名/ルール
   - 効果: タイプ+IP+名前+イラスト+効果+作品名/ルール
4. **レベル別装飾**: Lv2は薄金背景、Lv3は金枠+発光
5. **自信作バッジ**: HP高いがリスクありのキャラ
6. **絵文字アイコン**: 自動推測+手動指定、被り解消で連番(①②③)
7. **デッキ構築**: 同IDは最大4枚(主人公は1枚)、無制限フラグあり、追加カードリストは作品名/タイプ/全文検索でフィルタ可、共有コード(base64エンコード)で他人にデッキを送れる、デッキアイコン(主人公)1 枚必須の自動チェック&ワンクリック追加
8. **PDF印刷**: A4に9枚配置、トンボ付き
9. **ローカル保存**: localStorage(`card_forge_data_v4_tezuka`)
10. **テストプレイ(PLAYタブ)**: プレイマット(3モード)
    - **🧍 ソロ**: 2デッキ選択して両陣営を自分で操作
    - **🌐 ホスト/ゲスト (PeerJS二台対戦)**: ルームID(`cf-XXXXXX`)で別PC同士を接続、自サイドのみ操作可、相手の手札は伏せ枚数のみ表示
    - 自動シャッフル→初手7枚ドロー
    - 4ゾーン×2サイド: 手札・場・没案・山(自分の山top3は伏せプレビュー)
    - カードクリック→ポップオーバーでゾーン間移動・HP±・没にする
    - ホバーで原寸プレビュー(スキル/効果文を読める)
    - ターン進行(交代/フェーズ)、IP・原稿カウンタ(勝利10達成でトースト)
    - ターンログ(最大200件、A/B色分け)、リスタート、途中保存(ソロのみ)

## ゲームルール仕様(初稿)

- プレイヤー = 漫画家、勝利条件 = 原稿10枚で作品完成
- カード種類:
  - **デッキアイコン(旧主人公カード、第二稿)**: 進化なし、両面共通効果+裏面効果(覚醒)
  - キャラクター(脇役/名脇役/相棒/ライバル/イチオシ)
  - 展開アイディア
  - テコ入れ(伏せて相手ターンに発動)
  - どんでん返し(別山、アイコン覚醒時のみ使用)
- デッキ60〜80枚、同IDは最大4枚(一部無制限)
- **デッキアイコンカード**: 1 枚必須(レベル概念なし、デッキを識別する 1 枚固定カード)
- インスピレーションパワー(IP)/コスト: 没案ゾーンに送って支払う
- 「練り上げ」=レベルアップ、「没」=破壊

## ルール不明点(テストプレイで要検証)

1. コストとIPの関係明確化(用語統一推奨)
2. 持ち込みバトルで原稿が2枚になる条件
3. 「かばう」3コストのバランス
4. マリガンの厳しさ
5. 主人公スキルのコスト9は重すぎないか

## データ仕様

### Excelファイル(現在運用中)
ファイル名: `_仮_手塚カードゲーム__3_.xlsx` (77枚)
- 主人公カード(6枚): BJ・アトムの3段階×2
- キャラクター(34枚): 脇役14、名脇役12(自信作3含む)、相棒6、ライバル2
- 展開アイディア(17枚)
- テコ入れ(15枚)
- どんでん返し(5枚)

### 新Excelテンプレート(設計図準拠版)
ファイル名: `手塚カードゲーム_新テンプレート.xlsx`
- 技を「技名/コスト/効果」3列に分割
- ID、作品名、ルール列追加
- 自動マイグレーション機能あり

## 開発ノート

### 開発環境
- VS Code + Git + GitHub
- ローカルパス: `C:\Users\fd557\projects\card-forge\`
- デプロイ: `git add . → git commit → git push` で1-2分後にPagesに反映
- ハードリロード: `Ctrl+Shift+R`

### コード構造(index.html単一ファイル内)
- CSS: `<style>` タグ内 (約1000行)
- JavaScript: `<script>` タグ内 (約2300行)
- 全体: 約3800行
- 主要関数:
  - `buildCardHTML(c, idx, opts)` - カード描画(kind別に3レイアウト分岐)
  - `buildCardChip(inst, side, fromZone, faceDown)` - プレイマット用63×88pxチップ
  - `renderSkill(text, label)` - 技を「技名/コスト/効果」に分解表示
  - `importXLSX(ev)` - Excel読み込み(新旧両形式対応)
  - `generatePDF(mode)` - PDF生成(html2canvas経由で日本語対応)
  - `rebuildIconCache()` - 絵文字の被り解消(ベース絵文字単位で個別連番)
  - `migrateDecks()` - デッキ形式の自動移行(cardIds → cards[{cardId,count}])
  - `renderPlay()` / `renderPlaySide()` / `renderTurnBar()` - PLAYタブ描画
  - `startPlaySession()` / `endTurn()` / `moveInstance()` - プレイ進行
  - `showPlayHoverPreview()` - チップホバー時の原寸プレビュー

### 注意事項
- 日本語フォント問題はhtml2canvas経由で解決済み(jsPDFのテキスト埋込は不使用)
- localStorageのキー: `card_forge_data_v4_tezuka`(`state.playSession` も同キーに格納)
- 絵文字キャッシュ: ベース絵文字単位でグループ化、複数枚あれば1枚ごとに①②③連番(同名・別名問わず個別)
- BUILTIN_TYPES = [主人公(leveled), キャラクター(character), 展開アイディア(effect), テコ入れ(effect), どんでん返し(effect)]
- 並び替え時(ドラッグ・▲▼)は編集中カードのidを保持して新indexを再計算 — `state.cards[editingIdx]` のズレ防止
- PLAYのカードは `instanceId` で個別管理(同じcardIdの複数枚を区別)。場のキャラの現HPは `inst.hp` に保持
- PLAYのポップオーバー・ホバープレビューは `<body>` 直下に配置(renderPlay再描画で消えないように)
- PLAY二台対戦: PeerJS公開ブローカ(`0.peerjs.com`)+ Google公開STUNを使用、TURNなし。NAT越え失敗時はFirebase移行検討
- ネットワーク対戦中の操作はイベント送信(`netSendOp`)、受信側は `_isApplyingRemoteOp` フラグで再ブロードキャスト防止
- ネットワークsessionはリロードで復元しない(`loadData`で `network.mode !== 'solo'` ならクリア)
- ネット対戦時は `state.playSession.cardPool` に両者のカードを統合(相手のカードもここから検索)。`findPlayCard(cardId)` でプール優先、`state.cards` フォールバック
- HELLOで guest が deck instances + cards を送る、ホストが INIT で完全 playSession + cardPool を返す
- デッキ共有コード: `btoa(JSON({v:1, deck, cards}))`、取込時は cardId を全て新規発行して ID 衝突を回避

### カラーパレット(意味重視で再設計、旧色は自動マイグレーション)
- 主人公: `#a02d1f` (朱赤) — 漫画の主役・リーダー
- キャラ: `#2c5fa0` (群青) — 場で戦う仲間
- 展開: `#3d8a3a` (葉緑) — アイデア・物語の発展
- テコ入れ: `#c8761f` (橙) — 介入・伏せて発動
- どんでん返し: `#6e3a8b` (紫) — 大逆転・ドラマ
- 金: `#c9a961` / `#d4af37`

## 今後の予定

### 短期
- ✅ サイト上でのテストプレイ機能(PLAYタブ)実装済
- テストプレイで設計図レイアウトを実際に印刷して試す
- PLAYタブで実際に1ゲーム回してルール検証
- カードバランス調整(HP・コスト・ダメージの比率)
- 「かばう」「自信作」「テコ入れ」を段階的に有効化

### 中期
- ライバルキャラを増やす(現在2枚のみ)
- 展開アイディアカードのバリエーション追加
- ルール書を整理してテストプレイ用ドキュメント作成

### 長期
- ルールが固まったらREADMEを充実させて公開
- カードイラスト(本物)を入れる機能(画像アップロード)
